@model IEnumerable<WebApplication1.Models.Claim>

@{
    ViewData["Title"] = "Coordinator - Pending Claims";
}

<h2 class="mb-3">Pending Claims (Programme Coordinator)</h2>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">@TempData["SuccessMessage"]</div>
}
@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger">@TempData["ErrorMessage"]</div>
}

@if (!Model.Any())
{
    <div class="alert alert-info">No pending claims.</div>
}
else
{
    <table class="table table-striped table-bordered">
        <thead class="table-dark">
            <tr>
                <th>ID</th>
                <th>Lecturer</th>
                <th>Hours</th>
                <th>Rate</th>
                <th>Total</th>
                <th>Submitted</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var c in Model)
            {
                <tr>
                    <td>@c.ClaimId</td>
                    <td>@c.LecturerName</td>
                    <td>@c.HoursWorked</td>
                    <td>R @c.HourlyRate.ToString("N2")</td>
                    <td>R @((c.HourlyRate * c.HoursWorked).ToString("N2"))</td>
                    <td>@c.SubmissionDate.ToShortDateString()</td>
                    <td>
                        <a asp-action="ViewClaim" asp-route-id="@c.ClaimId" class="btn btn-sm btn-info">View</a>

                        <form asp-action="VerifyClaim" asp-route-claimId="@c.ClaimId" method="post" class="d-inline">
                            @Html.AntiForgeryToken()
                            <button class="btn btn-sm btn-success">Verify</button>
                        </form>

                        <form asp-action="RejectClaim" asp-route-claimId="@c.ClaimId" method="post" class="d-inline">
                            @Html.AntiForgeryToken()
                            <button class="btn btn-sm btn-danger">Reject</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
